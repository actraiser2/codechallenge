---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    app.dekorate.io/vcs-url: <<unknown>>
    app.dekorate.io/commit-id: f023ebc78ba1b3d1bb85e58bdd0548238b6c0be8
  labels:
    app.kubernetes.io/name: codechallenge
    app.kubernetes.io/version: v1
  name: codechallenge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: codechallenge
      app.kubernetes.io/version: v1
  strategy:
    rollingUpdate:
      maxSurge: "1"
      maxUnavailable: "1"
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: codechallenge
        app.kubernetes.io/version: v1
    spec:
      containers:
        - env:
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          envFrom:
            - configMapRef:
                name: codechallenge-config
          image: ghcr.io/actraiser2/codechallenge:1.0.4
          imagePullPolicy: Always
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /actuator/health/liveness
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 0
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 10
          name: codechallenge
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /actuator/health/readiness
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 0
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    app.dekorate.io/vcs-url: <<unknown>>
    app.dekorate.io/commit-id: f023ebc78ba1b3d1bb85e58bdd0548238b6c0be8
  labels:
    app.kubernetes.io/name: codechallenge
    app.kubernetes.io/version: v1
  name: codechallenge
spec:
  ports:
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: codechallenge
    app.kubernetes.io/version: v1
  type: ClusterIP
