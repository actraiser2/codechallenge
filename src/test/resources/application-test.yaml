spring:
  datasource:
    url: jdbc:h2:mem:codechallenge;IGNORECASE=TRUE
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    org: 
      springframework: info

santalucia:
  ams:
    cache:
      enabled: false

  
    